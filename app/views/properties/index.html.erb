<h1>All Properties</h1>

<p><%= link_to 'New Property', new_property_path, class: 'btn btn-info' %></p>

<ul>
<% @properties.each do |property| %>
  <li><%= property.name %> <%= link_to 'Delete', property_path(property), method: 'delete' %></li>
<% end %>
</ul>
